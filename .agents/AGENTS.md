# Project Rules

- **Auto Commit & Push:** After modifying code for a user request, always automatically review the changes, perform a `git add .`, `git commit -m "<appropriate message>"`, and `git push` unless the user explicitly opts out.
